<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_warehouse_form_inherit_allowed_users" model="ir.ui.view">
        <field name="name">stock.warehouse.form.inherit.allowed.users</field>
        <field name="model">stock.warehouse</field>
        <field name="inherit_id" ref="stock.view_warehouse"/>
        <field name="arch" type="xml">
            <xpath expr="//sheet" position="inside">
                <group string="Foydalanuvchi cheklovlari (Kirish huquqi)">
                    <field
                        name="allowed_user_ids"
                        widget="many2many_tags"
                        options="{'no_create': True, 'no_open': True}"
                        placeholder="Bo'sh qoldirilsa — barcha foydalanuvchilar ko'ra oladi"
                        colspan="2"
                    />
                </group>
            </xpath>
        </field>
    </record>
</odoo>