<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-name="pos_restaurant.ProductScreen" t-inherit="point_of_sale.ProductScreen" t-inherit-mode="extension">

        <xpath expr="//button[hasclass('pay-button')]" position="replace">
            <t t-if="this.pos.cashier.pw_disable_payment">
                <t t-if="!pos.scanning">
                    <button
                            class="btn-switchpane pay-button btn btn-lg w-50 lh-sm btn-primary pw_disable_button"
                            t-attf-class="{{ currentOrder.is_empty() ? 'disabled' : '' }}"
                    >
                        <span class="d-block">Pay</span>
                        <small><t t-esc="total" /></small>
                    </button>
                </t>
            </t>
            </xpath>

    </t>
</templates>
